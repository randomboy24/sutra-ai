backend/app/routes/health.py
@@ -1,5 +1,6 @@
from fastapi import APIRouter, HTTPException
from fastapi import APIRouter, Depends, HTTPException
@cubic-dev-ai
cubic-dev-ai Bot
8 hours ago
• 
Contributor
P2: Async handlers are doing synchronous SQLAlchemy I/O, which blocks the event loop. This is a scalability regression; keep handlers sync (def) or migrate to AsyncSession.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At backend/app/routes/health.py, line 18:

<comment>Async handlers are doing synchronous SQLAlchemy I/O, which blocks the event loop. This is a scalability regression; keep handlers sync (`def`) or migrate to AsyncSession.</comment>

<file context>
@@ -13,12 +14,14 @@
-def seed_health_data(clerk_user_id: str, body: SeedHealthRequest):
-    # TODO: Add Clerk JWT authorization to verify the caller owns clerk_user_id (IDOR protection)
+@router.post("/seed", response_model=SeedHealthResponse)
+async def seed_health_data(
+    body: SeedHealthRequest,
+    verified_user_id: str = Depends(get_current_user),
</file context>
Fix with cubic
@codeW-Krish	Reply...
frontend/components/dashboard/academic-health-panel.tsx
Comment on lines +87 to 91
    const token = await getToken();
    if (!token) return;
    setSeeding(true);
    setSeedError(null);
    try {
@cubic-dev-ai
cubic-dev-ai Bot
8 hours ago
• 
Contributor
P2: Token fetch is outside error handling, so auth token failures escape this handler. Keep token retrieval inside the try/catch and surface a user-facing seed error.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At frontend/components/dashboard/academic-health-panel.tsx, line 87:

<comment>Token fetch is outside error handling, so auth token failures escape this handler. Keep token retrieval inside the try/catch and surface a user-facing seed error.</comment>

<file context>
@@ -76,16 +77,19 @@ export function AcademicHealthPanel({
 
   const handleSeed = async () => {
     if (!clerkUserId) return;
+    const token = await getToken();
+    if (!token) return;
     setSeeding(true);
</file context>
Suggested change
    const token = await getToken();
    if (!token) return;
    setSeeding(true);
    setSeedError(null);
    try {
    setSeeding(true);
    setSeedError(null);
    try {
      const token = await getToken();
      if (!token) throw new Error("Authentication required to seed demo data");
Fix with cubic
@codeW-Krish	Reply...
backend/requirements.txt
fastapi>=0.100.0
sqlalchemy>=2.0.0
uvicorn>=0.20.0
psycopg2-binary>=2.9.0
@cubic-dev-ai
cubic-dev-ai Bot
8 hours ago
• 
Contributor
P2: psycopg2-binary is not recommended as a production dependency. Use source-built psycopg2 to avoid bundled native library security/stability issues.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At backend/requirements.txt, line 4:

<comment>`psycopg2-binary` is not recommended as a production dependency. Use source-built `psycopg2` to avoid bundled native library security/stability issues.</comment>

<file context>
@@ -0,0 +1,6 @@
+fastapi>=0.100.0
+sqlalchemy>=2.0.0
+uvicorn>=0.20.0
+psycopg2-binary>=2.9.0
+python-dotenv>=1.0.0
+clerk-sdk-python>=0.1.0
</file context>
Suggested change
psycopg2-binary>=2.9.0
psycopg2>=2.9.0
Fix with cubic
@codeW-Krish	Reply...
backend/app/routes/readiness.py
@@ -1,7 +1,8 @@
import json
@cubic-dev-ai
cubic-dev-ai Bot
8 hours ago
• 
Contributor
P2: Using async route handlers with synchronous SQLAlchemy sessions blocks the event loop. Keep these handlers sync (def) or migrate to AsyncSession to avoid concurrency regressions under load.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At backend/app/routes/readiness.py, line 20:

<comment>Using async route handlers with synchronous SQLAlchemy sessions blocks the event loop. Keep these handlers sync (`def`) or migrate to AsyncSession to avoid concurrency regressions under load.</comment>

<file context>
@@ -15,14 +16,16 @@
-def seed_readiness_data(clerk_user_id: str, body: SeedReadinessRequest):
-    # TODO: Add Clerk JWT authorization to verify the caller owns clerk_user_id (IDOR protection)
+@router.post("/seed", response_model=SeedReadinessResponse)
+async def seed_readiness_data(
+    body: SeedReadinessRequest,
+    verified_user_id: str = Depends(get_current_user),
</file context>
Fix with cubic
@codeW-Krish	Reply...
backend/app/auth/verify.py
    raise RuntimeError("CLERK_SECRET_KEY environment variable is not set")

clerk = Client(token=CLERK_SECRET_KEY)
security_scheme = HTTPBearer()
@cubic-dev-ai
cubic-dev-ai Bot
8 hours ago
• 
Contributor
P2: HTTPBearer() short-circuits on missing credentials, making the custom missing-header branch unreachable. Set auto_error=False if this function should own the 401 response.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At backend/app/auth/verify.py, line 12:

<comment>`HTTPBearer()` short-circuits on missing credentials, making the custom missing-header branch unreachable. Set `auto_error=False` if this function should own the 401 response.</comment>

<file context>
@@ -0,0 +1,29 @@
+    raise RuntimeError("CLERK_SECRET_KEY environment variable is not set")
+
+clerk = Client(token=CLERK_SECRET_KEY)
+security_scheme = HTTPBearer()
+
+
</file context>
Suggested change
security_scheme = HTTPBearer()
security_scheme = HTTPBearer(auto_error=False)
Fix with cubic
@codeW-Krish	Reply...
backend/app/auth/verify.py
    try:
        session = await clerk.verification.verify(credentials.credentials)
        return session.user_id
    except Exception:
@cubic-dev-ai
cubic-dev-ai Bot
8 hours ago
• 
Contributor
P2: except Exception masks operational failures as invalid-token 401s. This can hide auth-provider outages and make debugging/auth reliability issues harder.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At backend/app/auth/verify.py, line 28:

<comment>`except Exception` masks operational failures as invalid-token 401s. This can hide auth-provider outages and make debugging/auth reliability issues harder.</comment>

<file context>
@@ -0,0 +1,29 @@
+    try:
+        session = await clerk.verification.verify(credentials.credentials)
+        return session.user_id
+    except Exception:
+        raise HTTPException(status_code=401, detail="Invalid or expired token")
</file context>