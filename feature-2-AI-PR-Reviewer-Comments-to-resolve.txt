backend/app/routes/readiness.py
@@ -0,0 +1,133 @@
import json
@cubic-dev-ai
cubic-dev-ai Bot
now
Contributor
P1: Missing authentication/authorization on readiness routes causes IDOR; any caller can access or modify another user's readiness by changing clerk_user_id.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At backend/app/routes/readiness.py, line 18:

<comment>Missing authentication/authorization on readiness routes causes IDOR; any caller can access or modify another user's readiness by changing `clerk_user_id`.</comment>

<file context>
@@ -0,0 +1,133 @@
+router = APIRouter(prefix="/api/readiness", tags=["readiness"])
+
+
+@router.post("/seed/{clerk_user_id}", response_model=SeedReadinessResponse)
+def seed_readiness_data(clerk_user_id: str, body: SeedReadinessRequest):
+    # TODO: Add Clerk JWT authorization to verify the caller owns clerk_user_id (IDOR protection)
</file context>
@codeW-Krish	Reply...
frontend/components/dashboard/exam-readiness-panel.tsx
      await seedReadinessForUser(clerkUserId);
      setSeeded(true);
      refetch();
    } catch {
@cubic-dev-ai
cubic-dev-ai Bot
now
• 
Contributor
P2: Seed Demo Data errors are silently swallowed, so users get no feedback when seeding fails. The action can look broken on API/network failures.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At frontend/components/dashboard/exam-readiness-panel.tsx, line 121:

<comment>Seed Demo Data errors are silently swallowed, so users get no feedback when seeding fails. The action can look broken on API/network failures.</comment>

<file context>
@@ -0,0 +1,259 @@
+      await seedReadinessForUser(clerkUserId);
+      setSeeded(true);
+      refetch();
+    } catch {
+      // error handled silently
+    } finally {
</file context>
@codeW-Krish	Reply...
frontend/hooks/use-exam-readiness.ts
@@ -0,0 +1,56 @@
"use client";
@cubic-dev-ai
cubic-dev-ai Bot
now
Contributor
P3: New hook substantially duplicates existing hook flow, creating maintainability drift risk. Extract shared async-resource hook/helper and keep endpoint-specific calls thin.

Prompt for AI agents
Check if this issue is valid — if so, understand the root cause and fix it. At frontend/hooks/use-exam-readiness.ts, line 12:

<comment>New hook substantially duplicates existing hook flow, creating maintainability drift risk. Extract shared async-resource hook/helper and keep endpoint-specific calls thin.</comment>

<file context>
@@ -0,0 +1,56 @@
+  const [error, setError] = useState<string | null>(null);
+  const requestIdRef = useRef(0);
+
+  const fetchData = useCallback(async () => {
+    if (!clerkUserId) {
+      requestIdRef.current += 1;
</file context>